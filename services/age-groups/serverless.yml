service:
  name: hoepel-app-age-groups-service

plugins:
  - serverless-webpack
  - serverless-domain-manager
  - serverless-offline

custom:
  customDomain:
    domainName: ${file(../domain.yml):${self:provider.stage}}
    basePath: 'age-groups'
    stage: ${self:provider.stage}
    createRoute53Record: true

provider:
  name: aws
  runtime: nodejs8.10
  region: eu-west-3

functions:
  hello:
    handler: handler.hello
    events:
      - http:
          method: get
          path: hello
